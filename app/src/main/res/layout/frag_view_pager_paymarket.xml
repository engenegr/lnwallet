<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <GridView
        android:padding="@dimen/app_padding"
        android:verticalSpacing="@dimen/app_padding"
        android:horizontalSpacing="@dimen/app_padding"
        android:scrollbarStyle="outsideOverlay"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:clipToPadding="false"
        android:id="@+id/payLinks"/>

    <com.arlib.floatingsearchview.FloatingSearchView
        android:layout_margin="@dimen/app_padding"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        app:floatingSearch_searchHint="Search payment links"
        android:id="@+id/paySearch"/>

</FrameLayout>